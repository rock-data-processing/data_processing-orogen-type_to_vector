PROJECT(abstract_processing_component)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/.orogen/config")
INCLUDE(abstract_processing_componentBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

