# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(abstract_processing_componentTaskLib)
ADD_LIBRARY(${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_NAME} SHARED 
    ${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_SOURCES})

add_dependencies(${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ABSTRACT_PROCESSING_COMPONENT_TASKLIB_HEADERS}
    DESTINATION include/orocos/abstract_processing_component)

