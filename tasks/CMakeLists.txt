# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(general_processingTaskLib)
ADD_LIBRARY(${GENERAL_PROCESSING_TASKLIB_NAME} SHARED 
    ${GENERAL_PROCESSING_TASKLIB_SOURCES})

add_dependencies(${GENERAL_PROCESSING_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${GENERAL_PROCESSING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${GENERAL_PROCESSING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GENERAL_PROCESSING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GENERAL_PROCESSING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GENERAL_PROCESSING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GENERAL_PROCESSING_TASKLIB_HEADERS}
    DESTINATION include/orocos/general_processing)

