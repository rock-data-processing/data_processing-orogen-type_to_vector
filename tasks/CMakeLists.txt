# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(type_to_vectorTaskLib)
ADD_LIBRARY(${TYPE_TO_VECTOR_TASKLIB_NAME} SHARED 
    ${TYPE_TO_VECTOR_TASKLIB_SOURCES} VectorDataStorage.cpp)

add_dependencies(${TYPE_TO_VECTOR_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${TYPE_TO_VECTOR_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TYPE_TO_VECTOR_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TYPE_TO_VECTOR_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TYPE_TO_VECTOR_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TYPE_TO_VECTOR_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TYPE_TO_VECTOR_TASKLIB_HEADERS}
    DESTINATION include/orocos/type_to_vector)

